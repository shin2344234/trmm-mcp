[Unit]
Description=Run the MCP server backup nightly

[Timer]
# Ahead of the TRMM backup so the two do not overlap on IO.
OnCalendar=*-*-* 02:00:00
RandomizedDelaySec=10m
Persistent=true
Unit=mcp-backup.service

[Install]
WantedBy=timers.target
